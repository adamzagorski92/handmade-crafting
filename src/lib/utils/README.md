# How to build featcher function.

1. Add parameters:
   - url,
   - credential,
   - dataSetter,
   - errorSetter,
   - isPendingFlag,
   - signal

2. Set error bandling,
3. Set async function to get or mutate data,
